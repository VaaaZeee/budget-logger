import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.hu.budget.logger.szte',
  appName: 'Budget Logger',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;

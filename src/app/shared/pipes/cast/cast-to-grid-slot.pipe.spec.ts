import { CastToGridSlotPipe } from './cast-to-grid-slot.pipe';

describe('CastToGridSlotPipe', () => {
  it('create an instance', () => {
    const pipe = new CastToGridSlotPipe();
    expect(pipe).toBeTruthy();
  });
});
